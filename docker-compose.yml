services:
  web:
    build: .
    command: npm run dev -- --hostname 0.0.0.0
    ports: ["3000:3000"]
    volumes: [".:/app"]
    working_dir: /app
  api:
    build: ./backend
    ports: ["8000:8000"]
